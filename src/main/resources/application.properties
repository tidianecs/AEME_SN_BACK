# Server
spring.application.name=backend
server.port=8081

# Datasource
spring.datasource.url=jdbc:postgresql://localhost:5432/aeme_db
spring.datasource.username=user_db
spring.datasource.password=password_db
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.default_schema=public
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# OAuth2 Resource Server (Keycloak)
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8080/realms/aeme
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:8080/realms/aeme/protocol/openid-connect/certs

# Keycloak
keycloak.realm=aeme
keycloak.auth-server-url=http://localhost:8080
keycloak.client-id=aeme-client
keycloak.client-secret=i7pZ6Vcz8QtKxL2Z2llI3Qe3rHGwdx3k

# CORS
cors.allowed-origins=http://localhost:5173